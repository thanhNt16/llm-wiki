# Session transcript — 2026-09-12 (orders team)

> Agent session, excerpted. Human turns marked "H:", agent "A:".

H: We need durable order event delivery. Can we just use Redis pub/sub?

A: Checking the project notes — there is a postmortem: Redis pub/sub was
   rejected for order event delivery in April because messages are not
   durable (0.4% loss in staging soak).

H: Right, I forgot. What did we land on instead?

A: The 2026-09-10 platform sync decided on a transactional outbox for order
   event delivery: events are written in the same Postgres transaction as
   the order state change, then relayed.

H: Good. Let's implement the outbox relay next sprint.
